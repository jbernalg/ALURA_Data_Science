/*
Crear una función que reciba un número como parámetro y devuelva el 
resultado de multiplicar ese número por sí mismo.
*/

function numeroMultiplicado (num) {
    return num*num;
}

console.log(numeroMultiplicado(9));